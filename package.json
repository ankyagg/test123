{
  "name": "unified-alumni-portal",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "start": "node backend/server.js"
  },
  "dependencies": {
    "lucide-react": "^0.562.0",
    "react-calendar": "^6.0.0",
    "react-rating-stars-component": "^2.2.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
